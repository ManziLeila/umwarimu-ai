{
  "name": "umwarimu-ai-apps-script",
  "private": true,
  "type": "module",
  "description": "Clasp-managed Apps Script project: multi-school Sheets/Forms data layer and automation for Umwarimu AI.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "push": "npm run build && clasp push",
    "deploy": "npm run build && clasp push && clasp deploy",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83",
    "@types/jest": "^29.5.12",
    "esbuild": "^0.23.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.8.3"
  }
}
